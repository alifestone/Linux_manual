# Linux_manual
A manual that recording commands I usually use in Linux
<br><br>
The angle brackets `<>` indicate a placeholder. <br>Replace the text inside the brackets with your actual value (e.g., replace `<name>` with your name).
